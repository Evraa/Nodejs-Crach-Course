## What to learn?
    + express with view engines (EJS)


## Keywords
    + views (EJS)


## Requirement (npm install)
`sudo npm install -g nodemon`
`npm install express`
`npm install ejs`

## To Run:
`nodemon app.js`   